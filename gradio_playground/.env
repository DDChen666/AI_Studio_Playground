# Configure your Gemini credentials for local development.
# Copy this file to `.env` and set a valid API key value before launching the playground.

# Hugging Face Spaces and local runs both support this environment variable name.
GOOGLE_API_KEY=your-real-api-key
